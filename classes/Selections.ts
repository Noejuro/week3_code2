import TFigures from "../types/TFigures";

export default class Selections {
    selectedFigures: Array<TFigures>

    constructor(selectedFigures) {
        this.selectedFigures = selectedFigures;
    }

    updateConfiguration() {

    }

}